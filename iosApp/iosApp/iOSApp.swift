import SwiftUI
import Shared // Ensure Kotlin shared module is imported, name depends on Xcode setup (Shared/shared)

@main
struct iOSApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
