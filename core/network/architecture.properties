# This policy travels with the capability. Every field is required.
responsibility=Execute HTTP requests and expose transport-neutral responses and failures.
featureAccessible=false
dependencies=
publicInterfaces=NetworkPort
