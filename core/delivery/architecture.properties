# This policy travels with the capability. Every field is required.
responsibility=Resolve local-first audio delivery and manage a namespaced file cache through transport.
featureAccessible=false
dependencies=:core:network
publicInterfaces=DeliveryPort
