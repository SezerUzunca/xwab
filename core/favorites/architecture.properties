# This policy travels with the capability. Every field is required.
responsibility=Persist caller-owned namespaced favorite identifiers without interpreting content.
featureAccessible=true
dependencies=
publicInterfaces=FavoritesPort
